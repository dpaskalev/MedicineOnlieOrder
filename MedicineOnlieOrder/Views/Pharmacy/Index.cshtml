@model IEnumerable<Services.VewModels.PharmacyViewModel>

<h2>Pharmacies</h2>

<a asp-action="Create" class="btn btn-success mb-4">Add new Pharmacy</a>

<div>
    @foreach (var pharmacy in Model)
    {
        <partial name="_PharmacyIndexPartial" model="pharmacy" />
    }
</div>